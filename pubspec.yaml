name: gps_validator
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Agregar dependencias para ubicación
  geolocator: ^10.1.0  # Para obtener la ubicación del dispositivo
  location: ^5.0.0  # Para verificar el estado del GPS

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
